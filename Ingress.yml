apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nginxhttp
    annotations:
    kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "nginx"
    ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - example.com
      secretName: example-com-tls
  rules:
  - host: example.com
    http:
      paths:
      - backend:
          serviceName: nginxhttp
          servicePort: 80
