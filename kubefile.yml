apiversion: apps/v1
kind: deployment
metadata:
 name: nginx-deployment
   spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerport: 80
          NodePort: 30080
