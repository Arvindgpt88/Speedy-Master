apiversion: apps/v1
kind: deployment
metadata:
 name: nginx-deployment
 spec:
   container:
     -name: nginx-deployment
      image: nginx
      ports:
        -containerport: 80
         NodePort: 30080
